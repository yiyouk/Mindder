<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.mindder.file.model.mapper.FileMapper">
		<resultMap type="FileDto" id="file">
		<result column="file_idx" property="fileIdx" />
		<result column="save_folder" property="saveFolder" />
		<result column="original_file" property="originalFile" />
		<result column="save_file" property="saveFile" />

	</resultMap>
	<insert id="addFile" parameterType="FileDto">
		<selectKey keyProperty="idx" resultType="int" order="BEFORE">
			select max(file_idx)+1 from imagefile
		</selectKey>
		insert into imagefile(file_idx,save_folder,original_file,save_file)
		value(#{idx},#{saveFolder},#{originalFile},#{saveFile})
		
	</insert>
	<select id="findFile" parameterType="int" resultMap="file">
		select *
		from imagefile
		where file_idx =#{fileIdx}
	</select>
</mapper>